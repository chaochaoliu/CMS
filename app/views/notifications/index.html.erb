
<h1>通知</h1>
<div class="clear"></div>
    <ul class="news"> 
        <% @notifications.each do |notification| %>
          
          <li>
            <%= notification.created_at%> <br>
            <p><%= notification.content %></p>
          </li>
        <%end%>
    </ul>

    <ul class="news"> 
        <% @notifications.each do |notification| %>
          
          <li>
            <%= notification.created_at%> <br>
            <p><%= notification.content %></p>
            <div > 
                <%= image_tag notification.image.url%>
            </div>
          </li>
        <%end%>
    </ul>