<%= form_for(@sermon_sign_in) do |f| %>
  <% if @sermon_sign_in.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sermon_sign_in.errors.count, "error") %> prohibited this sermon_sign_in from being saved:</h2>

      <ul>
      <% @sermon_sign_in.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :sermon_id, :value => @sermon.id %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
