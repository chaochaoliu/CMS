
<h4>其他礼拜</h4>
<div class=" col-md-12 col-lg-12 "> 
<table class="table table-user-information">
  <thead>
    <tr>
      <th>题目</th>  
      <th>片长</th>
      <th>日期</th>
      <th>讲道人</th>
      <th>签到人数</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @other_services.each do |other_service| %>
      <tr>
        <td><%= link_to other_service.title, other_service %></td>
        <td><%= other_service.length %></td>
        <td><%= other_service.start_time.to_date %></td>
        <td><%= other_service.preacher.name %></td>
        <td><%= other_service.sign_in_count %></td>

      </tr>
    <% end %>
  </tbody>
</table>
</div>

<%= paginate @other_services %>
